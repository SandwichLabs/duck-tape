version: "2"
linters:
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  settings:
    goimports:
      local-prefixes:
        - github.com/SandwichLabs/duck-tape
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
